/**
 * Base props injected by A2UIRenderer into every catalog component.
 * Mirrors A2UIComponentProps from @a2ui-sdk/react internal types.
 */
export type BaseComponentProps = {
  surfaceId: string
  componentId: string
  weight?: number
}
